module.exports.add_account = function add_account(req, res) {
    // Account.add_account(req.swagger.params, res, next);
};

module.exports.delete_account = function delete_account(req, res) {
    // Account.delete_account(req.swagger.params, res, next);
};

module.exports.get_account_by_id = function get_account_by_id(req, res) {
    // Account.get_account_by_id(req.swagger.params, res, next);
};

module.exports.update_account = function update_account(req, res) {
    // Account.update_account(req.swagger.params, res, next);
};

module.exports.update_account_with_form = function update_account_with_form(req, res) {
    // Account.update_account_with_form(req.swagger.params, res, next);
};
